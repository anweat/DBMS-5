# 数据库管理系统（DBMS）技术选型文档

**版本**：3.0
**日期**：2026-04-19

---

## 当前技术更新：Qt 简单前端

当前一周冲刺新增 **Qt Widgets 简单前端**，后端 DBMS 引擎仍保持 C++17 自制实现，不引入外部数据库。Qt 仅负责图形界面展示和用户交互，不改变 SQL 解析、执行、存储等核心逻辑。

### 更新后的交互架构

```
Qt Widgets 前端 / CLI REPL
         │
         │ SQL 字符串 + Session
         ▼
┌──────────────────────────────────────┐
│            自制 DBMS 引擎             │
│  Lexer ──► Parser ──► AST            │
│                  │                   │
│             Executor                 │
│                  │                   │
│          Storage Managers            │
└──────────────────┬───────────────────┘
                   │ 本地文件 I/O
                   ▼
             data/ 数据目录
```

### Qt 前端选型

| 技术 | 用途 | 约束 |
|---|---|---|
| Qt Widgets | 主窗口、按钮、输入框、表格结果展示 | UI 简洁可用即可，不做复杂主题 |
| `QTableWidget` / `QTableView` | 展示 `QueryResult.rows` | 列名来自 `QueryResult.columns` |
| `QPlainTextEdit` | SQL 输入区 | 支持多行 SQL |
| `QStatusBar` / `QMessageBox` | 显示执行状态和错误 | 错误信息直接使用 `QueryResult.error.message` |

### 前端目录建议

```
src/
├── ui/
│   ├── README.md              # Qt 前端模块说明
│   ├── MainWindow.h/.cpp      # 主窗口：SQL 输入、执行按钮、结果表格
│   └── QtSessionAdapter.h/.cpp# Qt 层 Session 和 QueryResult 适配
```

> Qt 前端只依赖项目内 `DBEngine` 和 `types.h`，不得绕过引擎直接读写 `data/` 文件。

## 1. 总体架构

采用 **C++17 自制 DBMS 引擎 + CLI REPL + 简单 Qt Widgets 前端** 架构。CLI 继续作为基础交互入口，Qt 前端作为本周新增演示界面，两者都通过 `DBEngine::execute()` 调用同一套引擎。
**不依赖任何外部数据库**，所有数据由自制存储引擎管理，数据文件存储于本地磁盘。除 Qt 前端外，核心引擎仍只依赖 C++ 标准库。
CLI 可编译为命令行可执行文件；Qt 前端可作为单独 GUI 目标或后续扩展目标接入。

```
用户输入（stdin / 脚本文件 / Qt 界面）
         │
         ▼
┌─────────────────────┐
│ CLI REPL / Qt UI 层  │  ← SQL 输入 / 结果展示 / 错误提示
└──────────┬──────────┘
           │ SQL 字符串
           ▼
┌──────────────────────────────────────┐
│            自制 DBMS 引擎             │
│  Lexer ──► Parser ──► AST            │
│                  │                   │
│             Executor                 │
│                  │                   │
│          StorageEngine               │
└──────────────────┬───────────────────┘
                   │ 文件 I/O (fstream / 二进制)
           ┌───────▼───────┐
           │  本地数据文件  │
           │  .mdb  .tdf   │
           │  .trd  .tid   │
           └───────────────┘
```

**选择理由**：
- 核心引擎使用 C++ 标准库实现，CLI 可零额外依赖运行
- Qt Widgets 用于快速完成简单可演示前端，不进入存储和 SQL 语义层
- `std::fstream` 提供高效二进制文件 I/O，直接控制字节布局
- ANSI 转义序列实现彩色终端输出，无需第三方库
- 便于脚本化测试：`./dbms < test.sql` 直接批量执行
- 锻炼底层系统编程能力，与课程目标高度契合

---

## 2. 技术栈明细

### 2.1 CLI 交互层（C++ 标准库）

| 技术 | 来源 | 用途 |
|---|---|---|
| `std::cin` / `std::getline` | C++ 标准库 | 交互式 REPL 输入、多行支持 |
| ANSI 转义序列 | 内置（无依赖） | 终端彩色输出（错误红色、成功绿色、提示黄色） |
| 自制 TablePrinter | 项目内实现 | 终端表格渲染（查询结果格式化输出） |

### 2.2 自制引擎（C++17）

| 技术 | 版本 | 用途 |
|---|---|---|
| C++ 标准库 `<fstream>` | C++17 | 二进制文件读写（.mdb / .tdf / .trd / .tid） |
| `<string>` / `<vector>` / `<map>` / `<variant>` | C++17 | 数据结构与类型 |
| `<filesystem>` | C++17 | 目录创建、文件路径管理 |
| `<chrono>` | C++17 | 执行时间计量 |

> ⚠️ **不使用任何外部数据库驱动**（无 sqlite3 / leveldb 等）。
> 所有数据读写由自制 StorageEngine 模块通过 C++ `std::fstream` 完成。

### 2.3 自制 DBMS 引擎架构

```
SQL 字符串
   │
   ▼
┌──────────┐   vector<Token>  ┌──────────┐   ASTNode*  ┌───────────┐
│  Lexer   │ ───────────────► │  Parser  │ ──────────► │ Executor  │
└──────────┘                  └──────────┘             └─────┬─────┘
                                                             │
                                            ┌────────────────▼───────────────┐
                                            │         StorageEngine           │
                                            │   DatabaseManager               │
                                            │   TableManager                  │
                                            │   RecordManager                 │
                                            │   IndexManager (B+Tree)         │
                                            └────────────────┬───────────────┘
                                                             │ fstream 二进制 I/O
                                                   本地 .mdb/.tdf/.trd/.tid 文件
```

```cpp
// src/engine/DBEngine.h —— 引擎对外统一入口
class DBEngine {
public:
    QueryResult execute(const std::string& sql, Session& session);
    void beginTransaction(Session& session);
    void commit(Session& session);
    void rollback(Session& session);
};

// 文件格式约定
// .mdb  —— 系统目录文件（数据库列表、元信息）
// .tdf  —— 表定义文件（字段名、类型、约束）
// .trd  —— 表记录文件（定长二进制记录）
// .tid  —— 索引文件（B+Tree 节点页）
```

### 2.4 测试

| 技术 | 用途 |
|---|---|
| 自制轻量测试框架（assert 宏） | 单元测试（引擎各模块，无外部依赖） |
| 自定义 SQL 脚本 | 集成测试（批处理模式执行）|

---

## 3. 开发工具链

| 工具 | 说明 |
|---|---|
| C++17 编译器 | g++ 11+ / clang++ 13+ / MSVC 2022 |
| CMake | 3.15+，跨平台构建系统 |
| Git + GitHub | 版本控制 |
| GitHub Projects | 任务追踪 |
| VS Code / CLion | 推荐 IDE |

---

## 4. 目录结构

```
dbms-app/
├── CMakeLists.txt
├── src/
│   ├── main.cpp                          # 程序入口
│   ├── cli/                              # CLI 交互层
│   │   ├── Repl.h / Repl.cpp            # REPL 主循环（getline 驱动）
│   │   ├── Formatter.h / Formatter.cpp  # 结果格式化（表格、行数、耗时）
│   │   ├── MetaCommands.h / .cpp        # 元命令处理（\help \quit \desc 等）
│   │   └── Session.h                    # 会话状态（当前库、事务状态）
│   ├── engine/                           # 自制 DBMS 引擎核心
│   │   ├── lexer/
│   │   │   └── Lexer.h / Lexer.cpp      # 词法分析器（Token 定义 + 分词）
│   │   ├── parser/
│   │   │   ├── Parser.h / Parser.cpp    # 语法分析器（递归下降）
│   │   │   └── AST.h                    # AST 节点类型定义
│   │   ├── executor/
│   │   │   ├── Executor.h / .cpp        # 查询执行器（调度各 Manager）
│   │   │   └── ExprEvaluator.h / .cpp  # WHERE 条件求值
│   │   ├── storage/
│   │   │   ├── DatabaseManager.h / .cpp # .mdb 系统目录读写
│   │   │   ├── TableManager.h / .cpp    # .tdf 表定义文件读写
│   │   │   ├── RecordManager.h / .cpp   # .trd 记录文件读写
│   │   │   └── IndexManager.h / .cpp    # .tid B+Tree 索引
│   │   └── DBEngine.h / DBEngine.cpp    # 引擎对外统一入口
│   └── types.h                          # 共享类型（AST 节点、QueryResult、错误码）
├── data/                                 # 默认数据文件目录（运行时生成）
├── tests/                                # 单元测试 & 集成测试 SQL 脚本
└── README.md
```

---

## 5. 技术选型对比（备选方案）

| 方案 | 优点 | 缺点 | 结论 |
|---|---|---|---|
| **C++17 自制引擎 + CLI**（核心选定） | 直接控制内存与文件布局、CLI 编译产物即可运行 | 图形界面能力弱 | ✅ 核心保留 |
| **Qt Widgets 简单前端**（本周新增） | C++ 技术栈统一、表格和窗口控件成熟、适合快速演示 | 需要配置 Qt 环境 | ✅ 前端选定 |
| Node.js + TypeScript | 开发效率高、内置 readline | 不符合"原生 C++"要求 | ❌ |
| Java | 跨平台、I/O 性能好 | 非 C++，依赖 JVM | ❌ |
| Python | 快速开发、语法简洁 | 非 C++，性能弱 | ❌ |

---

## 6. 自制 SQL 子集范围

本项目实现以下 SQL 语法子集（类 MySQL 语法）：

| 类别 | 支持语句 |
|---|---|
| DDL | `CREATE DATABASE` / `DROP DATABASE` / `SHOW DATABASES` / `USE` |
| DDL | `CREATE TABLE` / `DROP TABLE` / `SHOW TABLES` / `DESCRIBE` |
| DDL | `ALTER TABLE ADD/MODIFY/DROP COLUMN` |
| DML | `INSERT INTO` / `SELECT` / `UPDATE` / `DELETE` |
| 查询 | `WHERE`（比较 + AND/OR）/ `ORDER BY` / `LIMIT` |
| 聚合 | `COUNT` / `SUM` / `MAX` / `MIN` / `AVG` |
| 约束 | `PRIMARY KEY` / `NOT NULL` / `UNIQUE` / `DEFAULT` / `FOREIGN KEY` |
| 索引 | `CREATE INDEX` / `DROP INDEX` |
| 事务 | `BEGIN` / `COMMIT` / `ROLLBACK` |
| 安全 | `CREATE USER` / `DROP USER` / `GRANT` / `REVOKE` |

## 7. CLI 元命令列表

| 元命令 | 说明 |
|---|---|
| `\help` | 显示帮助信息 |
| `\quit` / `\q` | 退出程序 |
| `\status` | 显示当前连接状态（当前库、事务状态） |
| `\desc <table>` | 快捷查看表结构 |
| `\use <db>` | 切换数据库 |
| `\databases` | 列出所有数据库 |
| `\tables` | 列出当前库所有表 |
| `\history` | 显示历史命令 |
| `source <file>` | 执行 SQL 脚本文件 |

# 数据库管理系统（DBMS）开发规划

## 当前冲刺更新：联表查询 + Qt 前端一周完成

**冲刺周期**：2026-05-06 至 2026-05-12
**冲刺目标**：在一周内完成联表查询能力补齐和简单 Qt 前端开发，前端以可演示、可查询、可展示结果为优先，不追求复杂 UI。
**交付口径**：后端支持稳定的多表 `SELECT` 查询，Qt 前端可连接本地 DBMS 引擎并完成登录/选择数据库/执行 SQL/展示结果/显示错误。

### 一周时间线

| 日期 | 目标 | 交付物 |
|---|---|---|
| Day 1 | 明确联表查询语法范围与 Qt 页面结构 | SQL 用例清单、Qt 页面草图、接口调用方式确认 |
| Day 2 | Parser/AST 支持表别名、字段限定名、多表 FROM | 联表查询解析单测 |
| Day 3 | Executor 支持两表/多表笛卡尔积 + WHERE 连接条件过滤 | 后端联表查询集成测试 |
| Day 4 | Qt 项目骨架、主窗口、SQL 输入区、结果表格 | 可启动的简单 Qt UI |
| Day 5 | 前端接入 DBEngine，显示 QueryResult 和错误信息 | UI 可执行 SQL 并展示结果 |
| Day 6 | 联表查询 + Qt 端到端联调，补充测试和边界用例 | Demo SQL 脚本、端到端截图/记录 |
| Day 7 | 代码冻结、PR 合并、README/接口/演示文档更新 | develop 合并完成，main 候选版本 |

### 本周优先级

| 优先级 | 范围 | 说明 |
|---|---|---|
| P0 | 主干可构建 | 当前 `cmake --build build` 在 Ninja/g++ 路径下失败，必须先恢复干净构建与可复现测试入口 |
| P0 | 联表查询正确性 | 支持 `FROM a, b WHERE a.id = b.a_id`、表别名、限定字段名、结果列命名 |
| P0 | Qt 基础前端 | 简单窗口即可：连接/登录区、数据库选择、SQL 编辑框、执行按钮、结果表格、错误提示 |
| P1 | 前后端接口稳定 | Qt 层直接调用 `DBEngine::execute()`，复用现有 `QueryResult`，避免重复解析结果字符串 |
| P1 | GitHub 协作规范 | 所有成员从 feature 分支提交 PR，禁止直接推 main，合并前必须能构建并通过相关测试 |
| P2 | UI 细节优化 | 结果表格列宽、状态栏、历史 SQL、常用示例按钮，可在 P0/P1 完成后补 |

### 代码审核发现的缺失项

| 模块 | 现状 | 缺失 / 风险 | 规划动作 |
|---|---|---|---|
| 构建 | 手工单文件编译可过，但 `cmake --build build` 经 Ninja 调用 g++ 失败且无诊断输出 | 主干不可验证，PR 合并缺少硬门禁 | Day 1 先修复构建入口，CI 使用干净 `cmake -B build && cmake --build build && ctest` |
| AST / Parser | `SelectNode` 只有 `table/database/tableAlias` 单表字段 | 不能表达多表 `FROM`，`FROM users u, orders o` 会在逗号处报错 | 将 `SelectNode` 改为 `fromTables` 列表，包含库名、表名、别名 |
| WHERE 表达式 | `WhereExpr` 有 `tableAlias`，但比较右值只能是字面量 | `u.id = o.user_id` 无法解析，联表连接条件无法表达 | 增加表达式右侧 `COLUMN_REF` 支持，允许列对列比较 |
| ExprEvaluator | 当前只用 `row.find(columnName)`，忽略 `tableAlias` | 同名字段会覆盖或查不到，无法判断字段歧义 | 建立限定键：`alias.column`、`table.column`、裸列歧义检测 |
| Executor SELECT | `execSelect()` 只扫描单表并按裸列投影 | 没有联表笛卡尔积/连接过滤/多表权限检查 | 增加多表行组合、WHERE 过滤、投影映射和多表权限检查 |
| SELECT * | 只展开单表列 | 多表 `*` 输出无法区分来源 | 多表场景输出 `alias.column` 或 `table.column` 表头 |
| ORDER BY / GROUP BY | 只解析裸列名 | 多表排序/分组字段可能歧义 | 支持限定字段名，歧义时报 `COLUMN_INVALID` 或 `COLUMN_NOT_FOUND` |
| RESTORE | Parser/AST 有 `RESTORE_DATABASE`，Executor 未分发也无实现 | 文档写了备份还原，但还原不可用 | 本周不作为 P0；列入 P2 或从文档中降级说明 |
| 索引 | `CREATE INDEX` 只创建空 `.tix`，没有回填已有记录；`SELECT` 不走 `lookup()` | 索引功能与 README “索引扫描”描述不一致 | 本周先不承诺索引优化；后续补回填和等值查询命中 |
| 显式 JOIN | 已补 `JOIN/INNER/ON` 语法链路和 Executor ON 条件过滤 | 外连接仍未实现 | 本周验收 `INNER JOIN ... ON`；外连接列入后续扩展 |
| IN 子查询 | `IN` 仅支持字面量列表 | 不能表达 `WHERE id IN (SELECT user_id FROM orders)` | 列入 P1：先支持单列、非关联子查询 |
| 存储过程 | 没有过程定义、系统目录、CALL 执行入口 | 缺少 DBMS 过程化封装能力 | 列入 P2：最小实现 `CREATE PROCEDURE` / `CALL` / `DROP PROCEDURE` |
| 触发器 | 没有触发器语法、元数据和 DML 钩子 | INSERT/UPDATE/DELETE 不能自动触发业务逻辑 | 列入 P2：先做 AFTER INSERT/UPDATE/DELETE |
| Qt 前端 | 当前只有 `src/ui/README.md`，没有 CMake/Qt 目标和窗口代码 | 还不能作为前端演示 | 成员B 新增 Qt Widgets 目标，最小 UI 先跑通 `DBEngine::execute()` |

### 修正后的一周落地顺序

| 顺序 | 任务 | 负责人 | 验收标准 |
|---|---|---|---|
| 1 | 修复/确认干净构建入口和 CI 命令 | 成员A | `cmake -B build && cmake --build build` 可复现；失败原因写入 issue |
| 2 | 修改 AST：`SelectNode.fromTables`、限定字段解析结构 | 成员C | Parser 单测覆盖单表兼容和多表 FROM |
| 3 | 支持 `WHERE a.id = b.user_id` 列对列比较 | 成员C + 成员E | 表达式 AST 可区分 literal 与 column ref |
| 4 | 实现多表扫描组合、别名字段映射、歧义报错 | 成员E | 两表/三表联查集成测试通过 |
| 5 | Qt 最小前端接入 | 成员B | UI 可执行联表 SQL 并展示表格 |
| 6 | GitHub PR 门禁和文档同步 | 成员A | PR 模板、CODEOWNERS、CI workflow、保护规则文档齐全 |

### 后端二期基建扩展

| 优先级 | 能力 | 建议范围 | 验收标准 |
|---|---|---|---|
| P0 已完成 | 显式 `INNER JOIN ... ON` | Parser 将 `JOIN` 表达为 `fromTables + onCondition`，Executor 复用当前多表组合与 WHERE 求值 | `SELECT u.name FROM users u INNER JOIN orders o ON u.id = o.user_id` 可执行 |
| P1 | `IN (SELECT ...)` 子查询 | 先支持单列、非关联子查询，把子查询结果集转成集合参与 WHERE 求值 | `WHERE id IN (SELECT user_id FROM orders)` 可执行并有测试 |
| P2 | 存储过程 | 系统目录保存过程名、参数和 SQL 语句块；`CALL` 逐句执行 | `CREATE PROCEDURE` / `CALL` / `DROP PROCEDURE` 有最小闭环 |
| P2 | 触发器 | 系统目录保存触发器定义；Executor 在 DML 成功后触发 AFTER 钩子 | AFTER INSERT/UPDATE/DELETE 可执行一条或多条 SQL |

> 下方原 4 周规划保留为项目整体背景；当前执行以本节一周冲刺为准。

**项目周期**：4周
**团队规模**：5人
**核心目标**：用**纯 C++ 原生**自主实现一套类 MySQL 语法的数据库管理系统，包含自制 SQL 解析器、查询执行器、二进制存储引擎，以及**命令行交互界面（CLI REPL）**，不依赖任何外部库
**里程碑**：第1周末中期验收（程序可编译运行 + 基础 DDL 可在命令行执行），第4周末正式提交

---

## 总体时间线

| 阶段 | 周次 | 目标 |
|---|---|---|
| 启动 & 架构设计 | Week 1 前半 | 环境搭建（CMake）、各模块接口定义、自制 SQL 子集确认 |
| **中期验收** | **Week 1 末** | **程序可编译后从命令行启动进入 REPL，能解析执行基础 DDL 并在终端输出结果** |
| 核心功能 | Week 2 | 完整 DML（CRUD）+ 存储引擎完善 + 完整性约束 |
| 高级功能 | Week 3 | 索引管理、事务控制、安全性、备份还原 |
| 测试 & 交付 | Week 4 | 集成测试、性能调优、文档完善 |

---

## Week 1（中期验收）——基础 DDL + CLI 连通

### 验收标准

- 执行 `cmake -B build && cmake --build build && ./build/dbms` 可进入交互式命令行提示符
- 能在 REPL 中解析并执行：`CREATE DATABASE`、`DROP DATABASE`、`SHOW DATABASES`、`USE`、`CREATE TABLE`、`SHOW TABLES`
- 简单 `SELECT` 语句可在终端以表格形式返回字段结果
- 执行出错时在终端打印清晰的错误信息

### 关键任务

| # | 任务 | 负责人 |
|---|---|---|
| 1 | 项目脚手架初始化（CMakeLists.txt，C++17），配置 Git 分支规范 | 成员A |
| 2 | 词法分析器（Lexer）——Token 类型定义 + 分词核心实现 | 成员C |
| 3 | 语法分析器（Parser）——DDL 部分（CREATE/DROP DATABASE/TABLE，SHOW，USE） | 成员C |
| 4 | AST 节点类型定义（`AST.h`，与 D/E 联合约定） | 成员C |
| 5 | 文件管理器 + 系统目录（`.mdb`）读写实现 | 成员D |
| 6 | CREATE / DROP DATABASE 存储层实现 + **DDL 执行器** | 成员D |
| 7 | CREATE TABLE / SHOW TABLES 存储层实现 + **DDL 执行器** | 成员D |
| 8 | 查询执行器框架（调度 AST 到 D/E 各处理函数） | 成员E |
| 9 | CLI REPL 主循环（`std::getline` 读取输入、分号检测、多行支持） | 成员B |
| 10 | 终端结果格式化（自制表格输出、行数提示、执行时间） | 成员B |
| 11 | 终端错误 & 提示信息输出（ANSI 颜色、格式） | 成员B |
| 12 | `\help` / `\quit` / `\status` 元命令支持 | 成员B |
| 13 | **中期验收演示准备 & 流程梳理** | 成员A |

---

## Week 2——核心功能（DML + 完整存储引擎）

| 功能模块 | 优先级 | 负责人 |
|---|---|---|
| Parser 扩展——DML（INSERT / SELECT / UPDATE / DELETE）语法 | A | 成员C |
| Parser 扩展——WHERE / ORDER BY / LIMIT 子句解析 | A | 成员C |
| 字段管理存储——ADD / MODIFY / DROP COLUMN（`.tdf` 更新） | B | 成员D |
| 记录插入——写入 `.trd` 二进制记录文件 | A | 成员D |
| 记录查询——从 `.trd` 读取并按 WHERE 过滤 | A | 成员D |
| 记录更新 & 删除 | B | 成员D |
| 查询执行器——DML 执行（INSERT / SELECT / UPDATE / DELETE） | A | 成员E |
| WHERE 条件求值引擎（比较运算 + AND/OR） | A | 成员E |
| 完整性约束——PRIMARY KEY / NOT NULL / UNIQUE / DEFAULT 校验 | A | 成员E |
| 终端分页浏览（结果超屏时按空格翻页） | B | 成员B |
| `\desc <table>` 表结构快捷命令 | A | 成员B |
| `\use <db>` / `\databases` / `\tables` 元命令扩展 | B | 成员B |
| CLI 操作日志（历史命令记录、`\history` 命令） | B | 成员B |
| Week 2 各模块单元测试 | — | 各自 |

---

## Week 3——高级功能

| 功能模块 | 优先级 | 负责人 |
|---|---|---|
| 索引存储——`.tid` 文件 B+Tree 结构实现 | C | 成员D |
| CREATE / DROP INDEX + 索引扫描替代全表扫描 | C | 成员E |
| 事务管理——BEGIN / COMMIT / ROLLBACK + undo log | C | 成员E |
| FOREIGN KEY 约束——参照完整性校验 | C | 成员E |
| 安全性管理——用户创建 / 权限分配（GRANT / REVOKE） | C | 成员E |
| 数据库备份（生成 SQL 文件导出） | C | 成员D |
| 数据库还原（解析并执行 SQL 文件） | C | 成员D |
| Parser 扩展——聚合函数（COUNT / SUM / MAX / MIN / AVG） | C | 成员C |
| CLI 批处理模式（`./build/dbms < script.sql` 非交互执行） | C | 成员B |
| Week 3 各模块单元测试 | — | 各自 |

---

## Week 4——测试 & 交付

| 任务 | 负责人 |
|---|---|
| 集成测试方案制定 + 端到端测试执行 | 成员A |
| SQL 语句兼容性测试（覆盖 50+ 测试用例） | 成员A |
| Bug 修复（Week 1~3 遗留问题） | 全员 |
| CLI 提示信息 / 错误处理完善 | 成员B |
| README（安装 / 运行 / 功能说明）+ SQL 语法速查手册 | 成员A |
| 代码最终审查 & 合并 main 分支 | 成员A |
| 项目演示 PPT | 成员A + 成员B |

---

## 风险与应对

| 风险 | 应对措施 |
|---|---|
| SQL 解析器工作量超出预期 | Week 1 仅解析基础 DDL；DML、聚合函数按优先级分批次迭代 |
| 二进制存储格式设计复杂 | Week 1 第1天统一确定文件结构（参考需求文档 3.12 节），固定长度记录先行 |
| 各模块接口对接困难 | Week 1 第1天冻结 AST 节点类型 + 存储引擎返回值格式，作为跨模块契约 |
| WHERE 嵌套逻辑复杂 | 先支持单条件与简单 AND/OR，复杂嵌套括号列为 C 级 |
| Week 4 时间紧 | Week 3 末功能冻结，Week 4 仅做测试与文档，不引入新功能 |
| 成员对 C++ 二进制 I/O 不熟悉 | Week 1 第1~2天提供 `fstream` 二进制读写示例代码模板 |
